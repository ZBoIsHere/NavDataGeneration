
-----

# obj2vln 项目

本项目 (Obj2VLN) 旨在处理视觉-语言-导航 (VLN) 相关的轨迹数据，将其从连续轨迹转换为拓扑路径，并进行数据标注。

## 📁 工作流程

### 1\. 轨迹转换 (`./transform`)

  * **文件：** `traj2topo.py`
  * **功能：**
      * 负责将连续的原始轨迹数据转换为基于拓扑图的路径。
      * 对转换后的路径进行优化，在尽量不改变原始导航意图的前提下，使路径表达更简洁。
  * **运行：** `python transform/traj2topo.py`


### 2\. 图像生成 (`./transform`)

  * **文件：** `Generate_topo_img.py`
  * **功能：**
      * 基于traj2topo.py生成的拓扑图路径文件，在habitat中渲染出图像，供后续prompt使用。
      * 生成的数据保存在data/topo_data中。
  * **运行：** `python transform/Generate_topo_img.py`

### 3\. 数据标注 (`./annotation`)

  * **文件：** `gemini_traj_annotation.py`
  * **功能：**
      * 使用上一步得到的数据，进一步构建完整prompt，调用gemini api进行标注。
      * 标注结果以txt文件的形式保存在data/anno_result中。
      * 在代码中先设置gemini api key
  * **运行：** `python annotation/gemini_traj_annotation.py`

-----

## 📊 配置

请按照以下步骤配置，并确保它们被放置在正确的目录中。

### 1\. HM3D 拓扑图数据

  * **下载地址：**
    ```
    https://huggingface.co/datasets/OpenGVLab/ScaleVLN/blob/main/r2r_preprocess_data.zip
    ```
  * **存放路径：** 下载后解压，将数据（`r2r_preprocess_data` 文件夹）放置于 `./data/` 目录下。

### 2\. 华为 HM3D 轨迹数据

  * **下载地址：** 
    ```
    obs://data-platform-shanghai/data/traj_datasets/objectnav/hm3d_v1_hd_l3mvn/train/content
    ```
  * **存放路径：** 下载 `content` 文件夹，并将其放置于 `./data/` 目录下。

### 3\. Habitat数据+环境
  * 按照官方教程安装Habitat-0.3.0相关环境
  * 下载HM3D v0.1场景数据集、HM3D Obj Nav V0.1数据集

-----

## 🌳 最终目录结构

完成数据准备后，项目目录应如下所示：

```
/obj2vln
├── /annotation             # 标注代码
    └── gemini_traj_annotation.py
├── /transform              # 轨迹转换与拓扑图图像生成代码
│   └── traj2topo.py
└── /data                   # 项目数据
    ├── /content            # 华为 HM3D 轨迹数据
    └── /r2r_preprocess_data    # HM3D 拓扑图数据
    └── /habitat_data       # Habitat自身的data文件夹软链接
        └── /datasets
            └── /objectnav
                └── /hm3d
                    └── /v1
                        └── /train
        └── /scene_datasets
            └── /hm3d
                └── /v0.1
                    └── /hm3d
                        └── /train
```

-----
